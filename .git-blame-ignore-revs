# Commits that only reformatted or mechanically rewrote code; `git blame`
# skips them and attributes lines to the change before.  GitHub reads this
# file automatically; locally run:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: ruff format the codebase at line-length 120
d3fd97b265a1c6802c91a605a1e2b3be997fc0b9
